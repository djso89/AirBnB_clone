#!/usr/bin/python3
"""
Base model module
"""

import json
import os


class Base:
    """Base class """

    def __init__(self):
        uid = 0
        # generate unique id and convert to string
        uid = uuid.uuid4()
        #convert uid to string
        self.id = str(uid)
